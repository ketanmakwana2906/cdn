docker run --rm -v my-models:/models ubuntu sh -c "\
  apt update && \
  apt install -y git && \
  git clone https://username:token@bitbucket.org/your-org/repo.git /models && \
  rm -rf /models/.git"
