package main

import (
    "github.com/gin-gonic/gin"
    "gin-crud/config"
    "gin-crud/models"
    "gin-crud/routes"
)

func main() {
    config.Connect()
    config.DB.AutoMigrate(&models.User{})

    r := gin.Default()
    routes.UserRoutes(r)
    r.Run(":8080")
}
