SELECT *
FROM train
WHERE is_active = 1
  AND (
    metadata_updated_on IS NULL
    OR metadata_updated_on <= CURDATE() - INTERVAL 28 DAY
  )
ORDER BY metadata_updated_on ASC
LIMIT 200;

ALTER TABLE train
ADD COLUMN metadata_updated_on DATE NULL;
